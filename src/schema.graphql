# import Post, Activity, ActivityType, ActivityCreateInput from "./generated/prisma.graphql"

type Query {
  me: User
  activities: [Activity!]!
}

type Mutation {
  # auth
  signup(email: String!, password: String!, name: String!): AuthPayload!
  login(email: String!, password: String!): AuthPayload!
  # activity
  createActivity(title: String!, type: ActivityType, location: String): Activity!
  attendActivity(id: String!): Activity!
  quitActivity(id: String!): Activity!
}

type AuthPayload {
  token: String!
  user: User!
}

type User {
  id: ID! 
  email: String! 
  name: String!
  posts: [Post!]!
  myActivities: [Activity!]!
  myTasks: [ActivityTask!]!
  attendedActivities: [Activity!]!
}
